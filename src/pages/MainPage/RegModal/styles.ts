import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    closeIcon: {
        backgroundColor: 'black',
        position: 'absolute',
        top: 20,
        right: 20,
        zIndex: 99999999,
    }
});
